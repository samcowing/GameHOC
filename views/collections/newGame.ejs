<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Add Game to Collection</title>
</head>

<body>
    <h1>Add Game to Collection Page</h1>
    <h3><%= collections.gameName %></h3>
    <h3><%= collections.gameId %></h3>
    <p>Choose the collection: </p>
    <form action="/addGame" method="POST">
        <select id="collection" name="collection">
            <% for (let i = 0; i < collection.length; i++) { %>
                <option value="<%=collection[i]._id%>"><%= collection[i].title%></option>
            <% } %>
        </select>
        <input type="submit" value="Add Games">
    </form>
</body>

</html>

